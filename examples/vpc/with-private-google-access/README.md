# With Private Google Access Example

Demonstrates a subnet with `private_ip_google_access = true`, enabling VMs without public IPs to reach Google APIs over internal routes.

## Usage

```bash
terraform init -backend=false
terraform validate
```
