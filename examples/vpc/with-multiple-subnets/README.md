# With Multiple Subnets Example

Demonstrates a multi-region VPC deployment with three subnets spread across `us-central1`, `us-east1`, and `europe-west1`.

## Usage

```bash
terraform init -backend=false
terraform validate
```
