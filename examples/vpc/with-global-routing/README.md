# With Global Routing Example

Demonstrates a VPC with `routing_mode = GLOBAL`, advertising dynamic routes to Cloud Routers in all regions for multi-region topologies.

## Usage

```bash
terraform init -backend=false
terraform validate
```
