# Basic VPC Example

Demonstrates the minimal `vpc_config` needed to provision a single VPC with one subnet using all default settings.

## Usage

```bash
terraform init -backend=false
terraform validate
```
